name: P1 Quota-Safe Daily Resume

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain rebaseline freeze
        run: |
          echo "Legacy 8-shard P1 daily resume is intentionally frozen."
          echo "The authoritative recommendation projection changed to the 16,380-title rebaseline."
          echo "Do not acquire quota locks or dispatch the obsolete 14,115-title writer."
          echo "A replacement quota-safe controller will be enabled only after the reviewed V3 rebaseline artifact is complete."
